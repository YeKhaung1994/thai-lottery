name: CI

on:
  push:
    branches: [main, redesign]
  pull_request:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: thai-lottery
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: thai-lottery/package-lock.json
      - run: npm ci
      - run: npm run lint
      - run: npm test
      - run: npm run build
